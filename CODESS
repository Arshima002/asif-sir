#include <stdio .h> 

int main( ) { int number, digit, result multiplier 	1 print f ( "Enter a five-digit number : 

scanf( "%d" , &number) ; 

print f ("New number is : result) ; return O; 

 for (int 1 digit = number % 10; digit = (digit + 1 ) % 1 0; // to ensure 9 becomes O result = result + digit * multiplier ; multiplier * 10 number 	10 ; 
